
- MYPATH in doc/Makefile.am
  introduced for two targets that have special build script snippet,
  and to make it work for both (a) development repo out-of-tree build and
  (b) makedist tarball build.

